/**
 * install jsonWebToken
 * jwt.sign(payload,secret,{expireIn:})
 * token client
 * 
 * 
 * 
 */


/**
 * set cookie sttpOnly
 * --------------------
 * set cookies with httpOnly .for development settings secure : false.
 * 
 * 
 * cors
 * 
 * 
app.use(cors({
  origin : ['http://localhost:5173'],
  credentials : true
}));
 * 
 * 
 * client side axios settings
 * in axios set credential : true
 * 
 */